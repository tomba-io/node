import { describe, it, expect } from "@jest/globals";
import { Finder } from "../lib/index";

describe("Finder Service", () => {
    it("should be defined", () => {
        expect(Finder).toBeDefined();
    });
});
