import { describe, it, expect } from "@jest/globals";
import { Status } from "../lib/index";

describe("Status Service", () => {
    it("should be defined", () => {
        expect(Status).toBeDefined();
    });
});
