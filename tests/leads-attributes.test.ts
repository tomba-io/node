import { describe, it, expect } from "@jest/globals";
import { LeadsAttributes } from "../lib/index";

describe("LeadsAttributes Service", () => {
    it("should be defined", () => {
        expect(LeadsAttributes).toBeDefined();
    });
});
