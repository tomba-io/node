import { describe, it, expect } from "@jest/globals";
import { Sources } from "../lib/index";

describe("Sources Service", () => {
    it("should be defined", () => {
        expect(Sources).toBeDefined();
    });
});
