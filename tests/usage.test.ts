import { describe, it, expect } from "@jest/globals";
import { Usage } from "../lib/index";

describe("Usage Service", () => {
    it("should be defined", () => {
        expect(Usage).toBeDefined();
    });
});
