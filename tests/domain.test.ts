import { describe, it, expect } from "@jest/globals";
import { Domain } from "../lib/index";

describe("Domain Service", () => {
    it("should be defined", () => {
        expect(Domain).toBeDefined();
    });
});
