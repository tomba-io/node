import { describe, it, expect } from "@jest/globals";
import { Keys } from "../lib/index";

describe("Keys Service", () => {
    it("should be defined", () => {
        expect(Keys).toBeDefined();
    });
});
