import { describe, it, expect } from "@jest/globals";
import { LeadsLists } from "../lib/index";
describe("LeadsLists Service", () => {
    it("should be defined", () => {
        expect(LeadsLists).toBeDefined();
    });
});
