import { describe, it, expect } from "@jest/globals";
import { Count } from "../lib/index";

describe("Count Service", () => {
    it("should be defined", () => {
        expect(Count).toBeDefined();
    });
});
